export type TCoursePost = {
  name: string;
  description: string;
  imageId: string;
};

export type TCourseUpdate = {
  name: string;
  description: string;
  imageId: string;
};

export type TCourse = {
  id: string;
  name: string;
  description: string;
  image: string;
  imageId: string;
};

export type TCourseFilters = {};
