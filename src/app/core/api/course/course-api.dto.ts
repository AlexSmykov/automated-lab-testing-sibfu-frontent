export type TCourseCreateDto = {
  name: string;
  description: string;
  // image: number | null;
};

export type TCourseUpdateDto = {
  name: string;
  description: string;
  // image: number | null;
};

export type TCourseDto = {
  id: number;
  name: string;
  description: string;
  // image: number | null;
};
